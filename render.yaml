services:
  - type: web
    name: bacnet
    env: python

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8

    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
