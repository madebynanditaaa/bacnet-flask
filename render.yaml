services:
  - type: web
    name: bacnet
    env: python

    # 👇 Python version (forces Render to use Python 3.11)
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8

    buildCommand: pip install -r requirements.txt

    # 👇 Correct entry point — your app.py file (no backend folder)
    startCommand: gunicorn -k uvicorn.workers.UvicornWorker app:asgi_app --bind 0.0.0.0:$PORT
